package com.example.managementsystem.salarycontrol.retirement_fund;

public class RetirementFundService
{
    /*private EmployeeDAO employeeDAO;
    private PeriodDAO periodDAO;
    
    public RetirementFundService(EmployeeDAO employeeDAO, PeriodDAO periodDAO) {
        this.employeeDAO = employeeDAO;
        this.periodDAO = periodDAO;
    }
    
    public double calculateRetirementFund(int employeeId, double taxRate) {
        Employee employee = employeeDAO.getEmployeeById(employeeId);
        double dailyWage = employee.getSalary() / 365;
        double baseRetirementFund = dailyWage * 30 * (periodDAO.getWorkingDays(employeeId) / 365.0);
        double taxAmount = baseRetirementFund * taxRate;
        return baseRetirementFund - taxAmount;
    }*/
}