package com.example.managementsystem;

public class RetirementFundControllerTest {
    
    /*@Test
    public void testProcessCalculation() {
        // Given
        RetirementFundService mockService = mock(RetirementFundService.class);
        when(mockService.calculateRetirementFund(eq(1), anyDouble())).thenReturn(1000.0);

        PrintStream originalOut = System.out;
        PrintStream mockOut = mock(PrintStream.class);
        System.setOut(mockOut);

        RetirementFundController controller = new RetirementFundController(mockService);

        // When
        controller.processCalculation(1);

        // Then
        ArgumentCaptor<String> captor = ArgumentCaptor.forClass(String.class);
        verify(mockOut).println(captor.capture());
        assertEquals("퇴직금: 1000.0", captor.getValue());

        System.setOut(originalOut);
    }*/
}